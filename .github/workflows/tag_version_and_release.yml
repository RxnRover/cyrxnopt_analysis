name: Tag Version and Release

on:
  push:
    branches:
      - main
  pull_request:
    types:
      - labeled

jobs:
  tag_version_and_release:
    uses: RxnRover/.github/.github/workflows/share/tag_version_and_release.yml@reusable_ci
